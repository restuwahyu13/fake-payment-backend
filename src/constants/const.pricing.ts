export enum EpackageType {
  FREE = 'free',
  BASIC = 'basic',
  PRO = 'pro',
  BUSINESS = 'business',
  ENTERPRISE = 'enterprise',
}

export enum EPeriodType {
  MONTHLY = 'monthly',
}

export enum ECurrencyType {
  USD = 'USD',
}
